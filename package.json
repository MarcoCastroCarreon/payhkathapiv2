{
  "name": "paykathapiv2",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun run build-functions.ts",
    "deploy": "bun run deploy-lambdas.ts",
    "full-deploy": "bun run build && bun run deploy"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.125",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.428.0",
    "@middy/core": "^4.6.5",
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1473.0",
    "mongoose": "^7.6.3",
    "zod": "^3.22.4"
  }
}
